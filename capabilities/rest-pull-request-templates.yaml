naftiko: 1.0.0-alpha2
info:
  label: Snyk API — Pull Request Templates
  description: 'Snyk API — Pull Request Templates. 3 operations. Lead operation: Delete pull request template for group. Self-contained
    Naftiko capability covering one Snyk business surface.'
  tags:
  - Snyk
  - Pull Request Templates
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    SNYK_API_KEY: SNYK_API_KEY
capability:
  consumes:
  - type: http
    namespace: rest-pull-request-templates
    baseUri: https://api.snyk.io/rest
    description: Snyk API — Pull Request Templates business capability. Self-contained, no shared references.
    resources:
    - name: groups-group_id-settings-pull_request_template
      path: /groups/{group_id}/settings/pull_request_template
      operations:
      - name: deletepullrequesttemplate
        method: DELETE
        description: Delete pull request template for group
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: group_id
          in: path
          type: string
          description: Snyk Group ID
          required: true
      - name: getpullrequesttemplate
        method: GET
        description: Get pull request template for group
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: group_id
          in: path
          type: string
          description: Snyk Group ID
          required: true
      - name: createorupdatepullrequesttemplate
        method: POST
        description: Create or update pull request template for group
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: group_id
          in: path
          type: string
          description: Snyk Group ID
          required: true
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: bearer
      token: '{{env.SNYK_API_KEY}}'
  exposes:
  - type: rest
    namespace: rest-pull-request-templates-rest
    port: 8080
    description: REST adapter for Snyk API — Pull Request Templates. One Spectral-compliant resource per consumed operation,
      prefixed with /v1.
    resources:
    - path: /v1/groups/{group-id}/settings/pull-request-template
      name: groups-group-id-settings-pull-request-template
      description: REST surface for groups-group_id-settings-pull_request_template.
      operations:
      - method: DELETE
        name: deletepullrequesttemplate
        description: Delete pull request template for group
        call: rest-pull-request-templates.deletepullrequesttemplate
        with:
          group_id: rest.group_id
        outputParameters:
        - type: object
          mapping: $.
      - method: GET
        name: getpullrequesttemplate
        description: Get pull request template for group
        call: rest-pull-request-templates.getpullrequesttemplate
        with:
          group_id: rest.group_id
        outputParameters:
        - type: object
          mapping: $.
      - method: POST
        name: createorupdatepullrequesttemplate
        description: Create or update pull request template for group
        call: rest-pull-request-templates.createorupdatepullrequesttemplate
        with:
          group_id: rest.group_id
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: rest-pull-request-templates-mcp
    port: 9090
    transport: http
    description: MCP adapter for Snyk API — Pull Request Templates. One tool per consumed operation, routed inline through
      this capability's consumes block.
    tools:
    - name: delete-pull-request-template-group
      description: Delete pull request template for group
      hints:
        readOnly: false
        destructive: true
        idempotent: true
      call: rest-pull-request-templates.deletepullrequesttemplate
      with:
        group_id: tools.group_id
      outputParameters:
      - type: object
        mapping: $.
    - name: get-pull-request-template-group
      description: Get pull request template for group
      hints:
        readOnly: true
        destructive: false
        idempotent: true
      call: rest-pull-request-templates.getpullrequesttemplate
      with:
        group_id: tools.group_id
      outputParameters:
      - type: object
        mapping: $.
    - name: create-update-pull-request-template
      description: Create or update pull request template for group
      hints:
        readOnly: false
        destructive: false
        idempotent: false
      call: rest-pull-request-templates.createorupdatepullrequesttemplate
      with:
        group_id: tools.group_id
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
